import React from "react";

const AddContact = (props) => {
  return (
    <div className="">
      <button type="button" name="" id="" className="btn btn-primary btn-lg mt-3 " onClick={props.metodo}>{props.name}</button>
    </div>
  );
}

export default AddContact;
