import React from "react";

const AddContact = (props) => {
  return (
    <div className="">
      <button type="button" name="" id="" className="btn btn-primary btn-lg " onClick={props.metodo}>Agregar</button>
    </div>
  );
}

export default AddContact;
